sources:
  - name: stripe 
    database: raw 
    schema: stripe 
    tables:
      - name: payment
        config:
          freshness:
            loaded_at_field: _batched_at
            warn_after: {count: 20, period: day}
            error_after: {count: 30, period: day}